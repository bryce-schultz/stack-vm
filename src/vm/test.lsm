push 0 # push i on to the stack
label1:
# ------- block -------
dup   # print i
print # print i
#----------------------
push 1
add # add: i + 1
dup # duplicate for persistance through comparison
push 10000000 # push the end factor
jl label1 # jump if i < 10
pop # pop the 10 off the stack
halt

# for (i = 0; i < 10; i++)
# {
#
# }